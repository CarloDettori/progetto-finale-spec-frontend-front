export type Game = {
    title: string,
    img: string,
    description: string,
    category: string,
    weightMB: number,
    genere: string[],
    price: number,
    goty: boolean,
    releaseDate: string,
    score: number
};